# PostgreSQL connection string for development, migrations, and database tests.
# Keep the real value in .env.local and never commit it.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require
